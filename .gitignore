node_modules

# Сборка установщика Electron (web/)
web/release/
# Keep environment variables out of version control
.env

/src/generated/prisma

# Локальные загрузки (не хранить в Git)
.local_uploads/

# Установщики для сайта — большие; кладите на сервер вручную, не в GitHub
web/public/files/*.exe
web/public/files/*.apk
web/public/files/*.apk.*

# Секреты фронта (Vite): только локально
web/.env
web/.env.local
web/.env.*.local
web/.env.txt
